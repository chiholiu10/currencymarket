import styled from "styled-components";
import theme from "../../Styles/Theme";

export const TitleHeaderTwo = styled.div`
  font-size: 20px;
  padding: 20px 0 15px;
  font-weight: ${theme.fontWeights.bold};
`;